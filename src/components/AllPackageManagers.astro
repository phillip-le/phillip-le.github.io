---
import type { PackageManagersProps } from 'starlight-package-managers';

type Props = PackageManagersProps;
---

<PackageManagers
  pkgManagers={['npm', 'yarn', 'pnpm', 'ni']}
  {...Astro.props}
/>